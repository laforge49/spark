(ns spark.core-test
  (:require
    [clojure.test :refer :all]
    [spark.core]
    [spark.debug :as debug]
    [spark.eval :as eval]
    [spark.kws :as kws]
    [spark.parse :as parse]
    ))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
